<%= form_for [:admin, @category] do |category| %>
  <%= render "shared/error_messages", object: category.object %>
  <%= category.label :name %>
  <%= category.text_field :name, class: "form-control" %>
  <%= category.submit yield(:button_text),
    class: "btn btn-primary form-control" %>
<% end %>
